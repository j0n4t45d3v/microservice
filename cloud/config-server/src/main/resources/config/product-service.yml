server:
  port: 8082

spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true
      path: /h2

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/